# ============================================================
# Fall-Prediction 依赖清单
# 建议用 conda / mamba 安装，PyTorch 与 mmcv 的 CUDA 版本必须匹配
# ============================================================

# ---------- 深度学习框架 ----------
# 参考实测环境：PyTorch 2.0.1 + cu118
torch>=2.0.1
torchvision>=0.15.2

# ---------- OpenMMLab 系列 ----------
# mmcv 需为与 torch/cuda 匹配的完整编译版
mmengine>=0.10.0
mmcv>=2.1.0
mmdet>=3.3.0
mmpose>=1.3.0

# ---------- 数值计算与图像处理 ----------
numpy>=1.24
opencv-python>=4.8

# ---------- 数据与标注 ----------
openpyxl>=3.1      # 读取 xlsx 标注文件

# ---------- 训练辅助 ----------
tqdm>=4.66

# ---------- 实时推理服务（可选） ----------
fastapi>=0.115
uvicorn>=0.34
websockets>=12.0

# ---------- RTMP/H.265 流自动转码（可选） ----------
imageio-ffmpeg>=0.4.9

# ---------- 数据核验可视化（可选） ----------
matplotlib>=3.7
